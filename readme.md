# Muncipales 2026

## run local

- install composer
- in `municipales/app` -> `composer install`
- install docker
- `docker-compose up`

## local mysql
`docker exec -it municipales-db-1 mysql -u nioufe -pdev_mysql_password regleselfvre`

## generate css

- `npm run sass`
- `npm run sass -- --watch`